# Deployment files are consumed by Linux — CRLF would break shebangs and
# systemd unit parsing, so pin them to LF regardless of the committer's OS.
*.sh            text eol=lf
*.service       text eol=lf
*.conf          text eol=lf
.github/**/*.yml text eol=lf
